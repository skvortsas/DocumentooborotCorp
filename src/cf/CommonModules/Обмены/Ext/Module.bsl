Процедура ПриЗаписиСправочникаПриЗаписи(Источник, Отказ) Экспорт
	ПодразделениеОбъекта = Источник.Подразделение;
	Выборка = ПланыОбмена.Риб.Выбрать();
	Пока Выборка.Следующий() Цикл
		Если ПланыОбмена.Риб.ЭтотУзел() = Выборка.Ссылка Тогда
			Продолжить;
		КонецЕсли;
		Если Выборка.Подразделение = ПодразделениеОбъекта Тогда
			ПланыОбмена.ЗарегистрироватьИзменения(Выборка.Ссылка,Источник.Ссылка);
		КонецЕсли;
	КонецЦикла;
КонецПроцедуры